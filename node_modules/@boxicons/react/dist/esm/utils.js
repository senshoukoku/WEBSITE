/**
 * Size to pixel mapping
 */
const SIZE_MAP = {
    'xs': 16,
    'sm': 20,
    'base': 24,
    'md': 36,
    'lg': 48,
    'xl': 64,
    '2xl': 96,
    '3xl': 128,
    '4xl': 256,
    '5xl': 512,
};
/**
 * Get pixel value from size preset
 */
export function getSizePixels(size) {
    var _a;
    return (_a = SIZE_MAP[size]) !== null && _a !== void 0 ? _a : SIZE_MAP['base'];
}
/**
 * Build transform attribute string from flip and rotate props
 */
export function buildTransform(flip, rotate) {
    const transforms = [];
    if (flip === 'horizontal') {
        transforms.push('scale(-1,1)');
    }
    else if (flip === 'vertical') {
        transforms.push('scale(1,-1)');
    }
    if (rotate !== undefined) {
        const degrees = typeof rotate === 'string'
            ? parseFloat(rotate.replace('deg', ''))
            : rotate;
        transforms.push(`rotate(${degrees})`);
    }
    return transforms.length > 0 ? transforms.join(' ') : undefined;
}
/**
 * Merge SVG props with defaults
 */
export function mergeProps(defaultProps, userProps) {
    const merged = { ...defaultProps };
    for (const [key, value] of Object.entries(userProps)) {
        if (value !== undefined) {
            merged[key] = value;
        }
    }
    return merged;
}
//# sourceMappingURL=utils.js.map